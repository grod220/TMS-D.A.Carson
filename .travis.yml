language: node_js
node_js:
  - "9.2"
before_script:
  - npm install -g firebase-tools
  - npm install
  - cd functions && npm install && cd ../
script:
  - npm run build
install:
  # - npm install -g firebase-tools
  # - cd functions && npm install
  # - npm install -g eslint eslint-plugin-promise eslint-config-react-app eslint-plugin-import
after_success:
  - firebase deploy --token $FIREBASE_TOKEN