language: node_js
node_js:
  - "9.2"
before_script:
  - npm install -g firebase-tools
  - npm install
  - cd functions && npm install && cd ../
script:
  - npm run build
after_success:
  - firebase deploy --token $FIREBASE_TOKEN